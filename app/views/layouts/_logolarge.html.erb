<div class="logo logo-large" style="margin-bottom: 32px;">
  <a href="/<%= current_application %>">
    <i class="fa fa-send-o"></i>
    <% if not defined?(with_text) or (defined?(with_text) and with_text) %>
      <span>tel100</span>
    <% end %>
  </a>
</div>