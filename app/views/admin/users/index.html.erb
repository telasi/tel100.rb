<div class="page-header">
  <h1><%= @title %> (<code><%= @users.count %></code>)</h1>
</div>

<!--table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>მომხ.სახელი</th>
      <th>სახელი, გვარი</th>
      <th>მობილური</th>
      <th>ელ.ფოსტა</th>
    </tr>
  </thead>
<% if @users.any? %>
  <% @users.each do |user| %>
    <tr>
      <td><code><%= user.username %></code></td>
      <td><%= link_to user.full_name, admin_user_url(id: user.id) %></td>
      <td>
        <% if user.mobile.blank? %>
          <span class="text-muted">(ცარიელი)</span>
        <% else %>
          <%= user.mobile %>
        <% end %>
      </td>
      <td>
        <% if user.email.blank? %>
          <span class="text-muted">(ცარიელი)</span>
        <% else %>
          <%= user.email %>
        <% end %>
      </td>
    </tr>
  <% end %>
<% else %>
  <tr>
    <td colspan="5" class="no-data">
      <% if @users.total_entries > 0 %>
        ამ გვერდზე არ არის ინფორმაცია მომხმარებლებზე.
        <%= link_to ' პირველ გვერდზე დაბრუნება', admin_users_url(page: 1) %>
      <% else %>
        არც ერთი მომხმარებელი
      <% end %>
    </td>
  </tr>
<% end %>
</table-->

<%
  empty_text = @users.total_entries == 0 ?
    'არც ერთი მომხმარებელი არაა რეგისტრირებული!' :
    "თქვენ არასწორ გვერდზე ბრძანდებით! გადადით #{link_to 'პირველ გვერდზე', admin_users_url(page: 1)}."
%>

<%= table_for @users, model_name: 'Sys::User', empty_text: empty_text do |t|
  t.text_field 'username', tag: 'code'
  t.text_field 'full_name', url: ->(x){ admin_user_url(id: x.id) }
  t.text_field 'mobile'
  t.text_field 'email'
end %>

<%= paginate @users, keyboardNavigation: true %>